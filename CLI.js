class CLI {
    constructor(gerenciadorContatos) {
        this.gerenciadorContatos = gerenciadorContatos;
    }

    // Métodos para interagir com o sistema via linha de comando
}

module.exports = CLI;
